<!DOCTYPE html>
<html>
  <head>
    <title>GrooveGrid</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous"> %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="main-container d-flex justify-content-between">
    <!--<div style="background-image: url('<%= image_path "Abstract_objects.png" %>')"> -->
    <%= render "shared/sidebar" unless devise_controller? || controller_name == "pages" && action_name == "home" || controller_name == "pages" && action_name == "welcome_page" || controller_name == "pages" && action_name == "welcome_video" || controller_name == "profiles" %>
    <%= render "shared/flashes" %>
    <% unless devise_controller? || controller_name == "pages" || controller_name == "profiles" %>
      <div data-controller="selectSong" class="container-content justify-content-start">
        <%= render "/shared/audio_player"%>
        <%= yield %>
      </div>
    <% else %>
      <div class="container-content justify-content-center">
        <%= yield %>
      </div>
    <% end %>

  </body>
</html>

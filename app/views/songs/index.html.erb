<div class="container">

  <h1>Today's Rhythm</h1>

<p>
  <div class="search-bar">
      <a class="active" href="#home">BPM</a>
      <a href="#about">Tag</a>
      <a href="#contact">Scale</a>
      <input type="text" placeholder="Search...">
  </div>
</p>

<p>
  <div class ="song-table-container">
    <table>
      <thead>
        <tr>
          <th>Song</th>
          <th>BPM</th>
          <th>Artist</th>
          <th>Scale</th>
          <th>Duration</th>
          <th>Tags</th>
        </tr>
      </thead>
      <tbody>
      <% @songs.each do |song| %>
        <tr data-action="click->music-player#select" data-song="<%= song.to_json %>" data-url="<%= cl_video_path song.audio_file.key || '' %>">
          <td><%= song.name %></td>
          <td class="primary-lavender-2"><%= song.bpm %></td>
          <td class="grey"><%= song.artist.name %></td>
          <td class="primary-lavender-2"><%= song.scale %></td>
          <td class="grey"><%= song.seconds %></td>
          <td class="grey"><span class="song-tag"><%= song.tags.name %></td>
        </tr>
      <% end %>
      <tbody>
    </table>
  </div>
</p>
